schema_version: 3.0
entity_type: model
required_fields:
  - model_id
  - schema_version
  - type
  - inputs
  - outputs
field_types:
  model_id: str
  type: str
validation_rules:
  - not_null: [model_id, type, inputs, outputs]
